/* PLOP INJECT_IMPORT */
//import React from "react";

/* PLOP_INJECT_CODE */

/**
 * L'instance du singleton TestSingleton.
 */
let instance;

/**
 * le state actuel du TestSingleton.
 */
let state = {
  //
  id: null,
};

/**
 * Le all mighty TestSingleton, qui nous permet un accès CRUD du ghetto.
 */
class TestSingleton {
  /**
   * Le constructeur
   * qui ne construira qu'un seul objet,
   * stocké dans instance.
   */
  constructor() {
    if (instance) {
      throw new Error("New instance of TestSingleton cannot be created.");
    }

    instance = this;
  }

  /**
   * Ceci permet d'obtenir le TestSingleton
   */
  GetID() {
    return state.id;
  }

  /**
   * Ceci permet de set des données dans le TestSingleton
   */
  SetID(newState) {
    state.id = newState;
  }

  /**
   * Ceci permet de ...
   */
  action_1({ arg1, arg2 }) {
    return;
  }

  /**
   * Ceci permet de ...
   */
  action_2({ arg1, arg2 }) {
    return;
  }
}

let TestSingletonInstance = Object.freeze(new TestSingleton());

export { TestSingletonInstance };
