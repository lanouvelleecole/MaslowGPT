export async function UpdateAPIUsage(req, stripe, customer) {
  const usageRecord = await stripe.subscriptionItems.createUsageRecord(
    customer.itemId,
    {
      quantity: 1,
      timestamp: "now",
      action: "increment",
    }
  );

  return usageRecord;
}
