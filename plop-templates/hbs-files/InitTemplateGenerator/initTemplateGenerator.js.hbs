/* PLOP_INJECT_IMPORT */

/*


Dans ce fichier:

renomme TestCommand en <command name pascalCase>
renomme TestCommand en <command name CamelCase>
renomme generatorName en <command name>
renomme Cette commande permet de ..... en une description textuelle de cette commande.

dans src/AllActions, copie/colle le dossier TestCommandActions, 
et renomme le en <command name camel>Actions.

dans src/AllActions/<command name camel>Actions, 
renomme le fichier TestCommandActions.js en <command name pascal>Actions.js
renomme le fichier initTestCommandAndCie.js en init<command name camel>AndCie.js

*/

import { TestCommandActions } from "../../AllActions/TestCommand/TestCommandActions.js";
import { TestCommandPrompts } from "../../AllPrompts/TestCommand/TestCommandPrompts.js";
// eslint-disable-next-line no-unused-vars
import { CommandNames } from "../../AppConstants/CommandNames.js";

function initTestCommandGenerator(plop, appRootPath) {
  /* PLOP_INJECT_CODE */

  plop.setGenerator(CommandNames.test_command, {
    description: "Cette commande permet de .....",

    /**
     * on demande à l'utilisateur
     * divers informations permettant la
     * création de notre chose.
     *
     */
    prompts: TestCommandPrompts(appRootPath),
    actions: function (data) {
      return TestCommandActions(data, appRootPath, plop);
    },
  });
}

export { initTestCommandGenerator };
