/**
 *
 * @param {*} pct
 * le pourcentage d'avancement du Work, de 0 a 100
 *
 * Callback permettant si besoin,
 * de faire des choses selon l'avancement du Work
 */
export const ProgressTrucsWork = ({ pct }) => {
  /*console.log(
    `Trucs Progress: ${JSON.stringify(pct)}`
  );*/
};
