/* PLOP_INJECT_IMPORT */

import { TestJobSingletonInstance } from "./TestJobSingleton";

/**
 *
 * @param {*} cancelData
 * les données d'annulation
 *
 * Permet de stocker des données d'annulation de Work
 */
export const GrabTestJobCanceller = (cancelData) => {
  /*console.log(
    `Let's grab the cancel data for TestJob`
  );*/

  TestJobSingletonInstance.AddCanceller({
    cancellerName: "TestJob",
    cancellerValue: cancelData,
  });
};
