// eslint-disable-next-line no-unused-vars
import { GetWorkWhere } from "src/services/DoWork/pieces/GetWorkWhere";
import { MakeHTTPRequest } from "src/services/MakeHTTPRequest/MakeHTTPRequest";
import { RequestTypes } from "src/services/MakeHTTPRequest/RequestTypes";

/**
 *
 * @param {*} work
 * les données de boulot individuel en cours d'exécution
 *
 * @param {*} workList
 * l'ensemble de tous les boulots à effectuer
 *
 * @param {*} onDone
 * callback à exécuter qd ce boulot individuel est réussi
 *
 * @param {*} onError
 * callback qd bobo durant boulot
 *
 * @param {*} workCancelGrabber
 * callback de récup de données de cancel work
 *
 * Ce boulot permet de ......
 */
export const TestJobWork = async ({
  work,
  workList,
  workCancelGrabber,
  onDone,
  onError,
}) => {
  /* Grab le Work de ....... 
  const specificWork = GetWorkWhere({
    workList,
    where: (work) => {
      return work.workWith == "";
    },
  });*/

  //console.log(`Work value: ${JSON.stringify(specificWork)}`);

  // la requête HTTP de ....
  MakeHTTPRequest({
    onGrabCanceller: workCancelGrabber,
    requestType: RequestTypes.GET,
    url: `https://reactnative.dev/movies.json`,
    onDone: onDone,
    onError: onError,
  });
};
